#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 integers
 * @h: The first integer
 * @a: The second integer
 * Return: Always 0 (Success)
 */

int mul(int h, int a)

{
	int result = h * a;

	return (result);
}
